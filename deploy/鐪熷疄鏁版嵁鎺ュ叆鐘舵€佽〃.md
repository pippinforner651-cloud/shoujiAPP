# E23跑起来 · 真实数据接入状态表

> 更新日期：2026-07-18。原则：未接入的一律写「预留/未接入」，不伪装。

## 一、账号与登录

| 项目 | 状态 | 说明 |
|---|---|---|
| 手机号+密码+邀请码注册 | ✅ 已接入（后端真实） | 注册即 pending，管理员审批后生效 |
| 管理员审批 | ✅ 已接入 | approve/reject，审批后跑量纳入汇总 |
| 微信授权登录 | 🔲 预留 | 登录页有正式流程说明，需微信开放平台资质 |
| 微信运动数据 | 🔲 不可直连（如实） | 微信运动数据仅支持在微信小程序内授权获取，外部 App 无接口；需开发配套小程序后接入 |
| 短信验证码登录 | 🔲 预留 | 当前无短信通道；测试验证码 123456 仅本机模式且页面明示 |

## 二、运动数据来源

| 来源 | 状态 | 说明 |
|---|---|---|
| Web GPS（浏览器前台） | ✅ 已接入 | accuracy>40m 漂移过滤；锁屏后台不支持（如实标注） |
| Android 原生 GPS（APK） | ✅ 已接入 | @capacitor/geolocation，权限拒绝有明示 |
| 手动补录 | ✅ 已接入 | 一律 pending，凭证必填，管理员审核计入 |
| 演示模式（室内） | ✅ 已接入 | source=sim 仅本机，**不上传服务端** |
| 华为运动健康 · 官方 OAuth 自动同步 | 🟡 mock_verified（八级状态制） | OAuth2 架构实现，仅模拟上游验证通过；需企业认证/AppGallery Connect/Health Kit 权限/包名签名/华为审核，详见《E23多平台运动数据适配器模拟验收报告》 |
| 佳明 Garmin · 官方 OAuth 自动同步 | 🟡 mock_verified（八级状态制） | Garmin Health API（OAuth 1.0a 三段式，Pull 模式，仅跑步计入）；需商业授权与评估环境；未获评估环境前不得声称端到端完成 |
| Apple Health | 🔲 不可直连（如实） | 仅 iPhone 真机 HealthKit 系统授权，无网页 OAuth；需 iOS 原生打包后按系统弹窗授权 |
| 悦跑圈 · GPX/TCX 轨迹导入 | ✅ 已接入 | 我的→设备与数据来源→悦跑圈；前端解析（src/lib/gpx.ts），带轨迹上传 source=joyrun，服务端按轨迹规则校验，合规直接 valid 计入 |
| 悦跑圈 · 凭证补录 | ✅ 已接入 | 日期/距离/时长+凭证说明（必填），source=joyrun 无轨迹一律 pending，管理员审核后计入 |
| 悦跑圈 · 官方开放平台 OAuth 自动同步 | 🟡 mock_verified（八级状态制） | OAuth2 架构实现，仅模拟上游验证通过；等待 Client ID/Secret/回调白名单/真实接口文档；未取得凭据前不得标记 connected |

## 三、服务端能力

| 能力 | 状态 | 说明 |
|---|---|---|
| 活动上传（幂等） | ✅ | clientId 唯一，重复返回 duplicated |
| 离线队列回补 | ✅ | 本机 outbox ≤50 条/批，失败保留 |
| 服务端校验引擎 | ✅ | 三态裁决，规则配置化；官方API数据走可信通道（仅服务端内部置位） |
| 多平台OAuth同步服务 | ✅（代码，mock_verified） | /api/providers/:provider 通用五端点；统一适配器契约（不支持的方法显式 not_supported）；令牌 AES-256-GCM 加密；state 一次性防重放；回调白名单；断开撤销+凭据清除 |
| 接入状态目录 | ✅ | GET /api/v1/integrations/catalog：八级状态 + 凭据/沙箱/生产分离；前端按钮唯一事实源 |
| 微信小程序数据提交接口 | 🟡 预留（未启用） | POST /api/v1/integrations/wechat-miniprogram/activities；未配置密钥一律 503 |
| 汇总重算唯一事实源 | ✅ | route_progress，仅 valid+approved |
| 排行榜 | ✅ | 仅 approved 成员 |
| 班级进度（含 serverTime） | ✅ | 前端展示数据时间戳 |
| 审计日志 | ✅ | 10 类事件留痕 |
| 管理后台界面 | 🔲 预留 | 目前用 API 直调（curl/Postman），页面未做 |

## 四、地图与路线

| 项目 | 状态 | 说明 |
|---|---|---|
| 环中国路线包 v2.2.0 | ✅ DRAFT | 27,171km / 176 节点；几何校验报告见前端「路线校验报告」 |
| 高德等正式地图服务 | 🔲 预留 | MAP_PROVIDER=static-pack，接口已留 |
| 全球跑地图包 | 🔲 预留 | 地图包导入/校验/回滚机制已就绪 |

## 五、第三方与推送

| 项目 | 状态 |
|---|---|
| 微信分享卡片 | 🔲 未接入 |
| 消息推送（完赛提醒等） | 🔲 未接入 |
| 数据统计埋点 | 🔲 未接入（无第三方 SDK，数据不外发） |
