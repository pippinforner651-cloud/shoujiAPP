# 悦跑圈真实联调清单（首个平台，本阶段唯一对象）

> 状态：**mock_verified**（模拟上游验证通过）。凭据到位前不得升级；填了 Client ID 也不自动升级——状态只能由管理员凭真实事件经 `POST /api/admin/integrations/joyrun/mark` 推进。

## 一、必须取得并配置（班级向悦跑圈申请）

| 项 | 现状 | 说明 |
|---|---|---|
| 官方 Client ID | ❌ 未取得 | open.thejoyrun.com 企业/组织主体申请 |
| 官方 Client Secret | ❌ 未取得 | 只存服务器 .env，不入库不入仓 |
| 正式回调地址 | ❌ 未登记 | `https://api.你的域名/api/providers/joyrun/callback`，需在悦跑圈登记白名单 |
| 授权范围（scope） | ❌ 待官方文档 | 代码当前按公开资料默认；以正式文档为准调整 `getAuthorizationUrl` |
| 测试用户 | ❌ 未指定 | 需 1-2 个真实悦跑圈账号 |
| 正式接口端点 | ❌ 待官方文档 | 代码默认 `https://open.thejoyrun.com`，`JOYRUN_API_BASE` 可覆盖 |
| Token 刷新规则 | ❌ 待官方文档 | `refreshAuthorization` 已实现 refresh_token 模式；若官方规则不同只改一处 |
| 分页规则 | ❌ 待官方文档 | 当前按日逐日拉取（run/detail/date）；若有分页参数在 `pullActivities` 内补齐 |
| 数据字段说明 | ❌ 待官方文档 | 字段差异只改 `normalizeActivity` 单点函数 |

## 二、16 步真实验收对照表（凭据到位后逐项执行）

| # | 验收项 | 代码支撑 | 模拟态已验证 |
|---|---|---|---|
| 1 | 用户登录 E23 | auth 模块 | ✅ |
| 2 | 点击悦跑圈授权 | ProviderSyncPanel（状态达 sandbox_connected 后按钮开放） | ✅ |
| 3 | 跳转真实官方授权页 | getAuthorizationUrl | ⬜ 需真实端点 |
| 4 | 用户完成授权 | 悦跑圈侧 | ⬜ 需测试用户 |
| 5 | 服务端收到真实回调 | /callback（state 一次性防重放） | ✅ |
| 6 | Token 加密落库 | AES-256-GCM（DB 无明文，测试 24） | ✅ |
| 7 | 拉取 ≥1 条真实跑步记录 | pullActivities | ⬜ 需真实调用 |
| 8 | 标准化写入 activities | normalizeActivity + createOne | ✅（模拟数据） |
| 9 | 只接受跑步类型 | 当前悦跑圈端仅跑步；佳明侧已实现类型过滤 | ✅ |
| 10 | 更新个人里程 | recomputeProgress | ✅ |
| 11 | 更新全班进度 | 班级汇总唯一事实源 | ✅ |
| 12 | 更新排行榜 | /api/class/leaderboard | ✅ |
| 13 | 第二次同步不重复累计 | 幂等键 joyrun-{runId} | ✅ |
| 14 | Token 过期可刷新 | refreshAuthorization | ✅（模拟）/⬜ 真实规则待文档 |
| 15 | 断开后凭据清除 | disconnect（撤销+清除+state 清除） | ✅ |
| 16 | 日志不出现明文 Token | 全链路无令牌日志（断言保障） | ✅ |

⬜ = 依赖真实凭据/真实端点，当前无法执行。

## 三、联调执行顺序（凭据到位后）

1. 服务器配好 `JOYRUN_*` 四个变量 + `CALLBACK_ALLOWED_HOSTS`；
2. 管理员标记 `sandbox_connected`（附申请获批凭证 note）→ 前端按钮变「测试连接」；
3. 1 名测试用户走完整 16 步；
4. 核对 `normalizeActivity` 与真实返回字段（有差异只改该函数）；
5. 2-3 人试点 → 标记 `pilot_verified` → 全班推广 → `generally_available`。
