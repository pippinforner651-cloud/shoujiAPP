# E23真实云端与悦跑圈首个平台联调报告

> 日期：2026-07-20 · 分支：kimi/e23-v2-product
> 核心结论：**后端尚未上线（无云资源凭据）；悦跑圈真实联调未完成（无官方凭据）。** 本阶段已把"资源到位即可执行"的全部工程准备做完，并冻结新增平台适配器。

## 〇、SHA 核验（交付一致性）

| 项 | 值 |
|---|---|
| 本地 HEAD 完整 SHA | 交付时 `git rev-parse HEAD` 实测值，与源码包文件名一致（本报告入库后 HEAD 再推进一位属正常自指现象，故以包文件名/交付消息为准） |
| origin/kimi/e23-v2-product | **不可得**：沙箱内仓库从未配置 origin 远程，无 GitHub 凭据（git fetch origin → "does not appear to be a git repository"）。需提供仓库 URL + 写权限后执行首次 push |
| 源码包 HEAD SHA | 包文件名即最终 HEAD 完整 SHA |
| GitHub Actions 验证 SHA | **不可得**：无远程无法触发 Actions；三个工作流已通过本地静态 YAML 校验 + 本地代理跑测（见 CI 节） |

> 关于 714fc2d / 2f9a77b：2f9a77b 为上一阶段手工修正提交；714fc2d 为随后交付快照在同一分支上生成的提交（现 HEAD 的祖先），两者同一直线历史相邻提交，非分叉。

## 一、远程分支取证（原样摘录）

```
$ git branch --show-current        → kimi/e23-v2-product
$ git remote -v                    → （空，无远程）
$ git fetch origin                 → fatal: 'origin' does not appear to be a git repository
$ git rev-parse HEAD               → 714fc2d21cf71e0c3fb5a1d795a40af25add4097（本阶段起点）
$ git rev-parse origin/kimi/e23-v2-product → NO_REMOTE_BRANCH
$ git status                       → ## kimi/e23-v2-product（干净）
```
开发基于既有分支 kimi/e23-v2-product 线性推进；未建孤立历史；未修改 main；未 force push。

## 二、云端后端部署（工程就绪清单，资源到位即上线）

| 项 | 状态 | 实现 |
|---|---|---|
| PostgreSQL | ✅ 编排就绪 | postgres:16-alpine + 数据卷 + healthcheck |
| 后端 API | ✅ 镜像就绪 | 两阶段 Dockerfile；启动先 migrate 再 server |
| HTTPS | ✅ 配置就绪 | Caddy 反代自动签发证书（deploy/Caddyfile，待真实域名） |
| 独立 API 域名 | ❌ 待购买 | 域名+DNS A 记录（见 云端资源清单） |
| 数据库迁移 | ✅ | prisma migrate deploy（deploy.sh 第 3 步） |
| 健康检查 | ✅ | GET /api/health（db/serverTime） |
| 日志 | ✅ | Fastify JSON 日志；Caddy JSON 访问日志；不打印令牌 |
| 备份方案 | ✅ 脚本就绪 | deploy/pg_backup.sh：pg_dump+gzip+保留 14 份+对象存储异地预留 |
| 环境变量管理 | ✅ | deploy/环境变量清单.md；.env 不入仓 |
| CORS 白名单 | ✅ | 生产缺省拒绝跨域，必须显式配置 |
| 限流 | ✅ | @fastify/rate-limit 全局限流（默认 300/分/IP，可配） |
| Token 加密密钥 | ✅ | TOKEN_ENCRYPTION_KEY 生产必填，缺省拒绝回退 |
| JWT 密钥 | ✅ | JWT_SECRET 必填（compose :? 强制） |
| 对象存储接口预留 | ✅ | services/storage.ts：未配置如实 not_supported |
| 前端/Android 接 API | ✅ | 统一 VITE_API_BASE_URL；为空=本机模式不伪装 |
| GitHub Pages 当后端 | 🚫 未如此 | Pages 仅前端静态托管 |
| 生产密码入仓 | 🚫 无 | compose 无硬编码密码（断言保障） |

## 三、悦跑圈真实接入（首个平台）

**状态：mock_verified（未升级）。** 九项必备凭据/文档全部未取得（见 docs/悦跑圈真实联调清单.md 第一节）；16 步真实验收中 12 步已有代码支撑并经模拟验证，第 3/4/7/14 步依赖真实凭据无法执行。

**悦跑圈真实联调未完成。**

状态推进规则已固化：填 Client ID 不自动升级；只能由管理员凭真实事件经 `POST /api/admin/integrations/joyrun/mark` 留痕推进（sandbox_connected → … → generally_available）。

## 四、CI 验收（如实）

| 工作流 | 本地核验 | Actions 运行链接 |
|---|---|---|
| Kimi Android Preview（E23 Android 预览版 APK / build-apk） | YAML 校验通过 | 不可得（无远程无法触发） |
| Kimi PWA Pages（build-deploy） | YAML 校验通过 | 不可得（同上） |
| Kimi Backend CI（backend-test） | YAML 校验通过 + 本地代理全绿 | 不可得（同上） |

本地代理跑测（与 backend CI 同步骤）：
- 测试数量：**后端 31/31 集成测试通过**（真实内嵌 PG17）；前端单测 82/82（本报告出具时点数；第七阶段新增 14 条断言后，当前 `npm run test:unit` 为 96/96，以 docs/测试报告.md 口径表为准）；移动端 4 宽度 ×24 断言全过
- Docker 构建结果：**未执行**（沙箱无 Docker）；Dockerfile 经静态审查，CI 环境（ubuntu-latest）具备 Docker
- 数据库迁移结果：prisma db push/migrate 在 31 项集成测试与历次 HTTP 抽查中真实执行成功
- API 集成测试结果：31/31（含限流 429、CORS、存储预留、state 防重放、令牌密文、catalog、webhook 501、小程序 503、断开撤销）

## 五、报告要求字段汇总

| 字段 | 值 |
|---|---|
| 最终完整 SHA | 交付时实测 HEAD（=源码包文件名，交付消息给出） |
| 远程分支 SHA | 不可得（无 origin） |
| 源码包 SHA | 与本地 HEAD 一致（包文件名） |
| API 公网地址 | 无（后端未上线） |
| 健康检查地址 | 无（后端未上线）；上线后为 `https://api.你的域名/api/health` |
| 数据库状态 | 未部署；迁移脚本与种子就绪，集成测试内嵌 PG 验证通过 |
| 悦跑圈凭据状态 | 未取得（Client ID/Secret/回调白名单/scope/端点/刷新/分页/字段文档 全部待官方） |
| 官方授权状态 | 未发生 |
| 真实测试用户状态 | 未指定 |
| 真实同步记录 ID | 无（未真实同步） |
| 同步里程 | 0（真实通道） |
| 幂等复测结果 | 模拟态通过（joyrun-{runId} 幂等键，重复同步 duplicated） |
| Token 加密验证 | 通过（DB 无明文，集成测试 24） |
| 断开授权验证 | 通过（撤销+凭据清除+state 清除，集成测试 28） |
| Android 连接结果 | 未连接（无公网 API）；VITE_API_BASE_URL 构建开关就绪 |
| PWA 连接结果 | 未连接（同上） |
| GitHub Actions 链接 | 不可得（需仓库推送权限） |
| 尚未完成事项 | ①云服务器+域名采购 ②后端上线 ③悦跑圈凭据申请与回调登记 ④16 步真实验收 ⑤GitHub 推送与 Actions 首跑 ⑥真实测试用户授权试点 |

## 六、班级下一步（按序）

1. 采购云服务器+域名（清单：deploy/云端资源清单.md）→ bash deploy/deploy.sh 上线；
2. 推送 GitHub（提供仓库 URL+凭据）→ 三工作流首跑取 Actions 链接；
3. 申请悦跑圈凭据 → 登记回调白名单 → 服务器配置 JOYRUN_*；
4. 管理员标记 sandbox_connected → 1 名测试用户跑 16 步 → 逐项销 ⬜；
5. 试点 → pilot_verified → 全班 generally_available。
