# 测试报告（Kimi 独立交付线 · 最终版）

> 最后更新：第七阶段（真实仓库接入准备）收尾，全部为沙箱内真实执行结果。

## 环境（如实声明）

- 后端测试：vitest + embedded-postgres **真实 PG 17**（测试库自动迁移+种子），上游平台 API 为**模拟上游**（非真实悦跑圈/华为/佳明服务器）。
- 移动端测试：无头 Chromium，iPhone 参数（触屏模式、iOS Safari UA、2/3 倍屏）。
- **未在真机 iPhone 上测试；未真实编译 APK；未真实推送 GitHub；Actions 未真实运行；悦跑圈真实联调未完成。**

## 测试命令与数字对照表（唯一口径，消除歧义）

| 数字 | 命令（执行目录） | 内容 |
|---|---|---|
| **103/103** | `npm run test:unit`（仓库根目录）= `node scripts/unit_test.mjs` | 全项目静态与单元断言（八阶段 103 条） |
| **31/31** | `npm test`（= `test:integration`，backend/ 目录）；CI 用 `npm run test:ci`；内置库调试用 `npm run test:embedded` | 后端 API 集成测试（外部 PG 优先；embedded-postgres 仅开发 fallback；模拟上游） |
| **4×24=96 项** | `node scripts/mobile_test.cjs`（仓库根目录） | 移动端四宽度响应式断言 |

- 接入脚本（integrate-kimi-backend.ps1）第 13 步实际执行（在 backend/ 目录内）：`npm run typecheck` → `npm run lint` → `npm test`（= `test:integration`，31 项）→ `npm run build`。**不执行**根目录 test:unit。Windows 本机若无 TEST_DATABASE_URL，第 13 步将以可读提示明确失败（不伪造通过）。
- Kimi Backend CI 实际执行：`npm ci` → prisma generate → `npm run typecheck` → `npm run lint` → Prisma 迁移（postgres:16-alpine service，`DATABASE_URL`）→ `npm run test:ci`（31 项，测试进程读 `TEST_DATABASE_URL` 指向同一 service，**不启动 embedded-postgres**）→ `npm run build` → `docker build`。**不执行**根目录 test:unit。
- 验收口径：后端接入验收以 **vitest 31/31 + typecheck/lint 零错误**为准（脚本第 13 步与 CI 即此套）；全项目静态验收以 **test:unit 103/103** 为准；移动端以 **4×24** 为准。
- 历史说明：早期文档曾出现 82/82，系第六阶段末旧数字（当时 82 条断言；第七阶段新增 14 条后为 96 条，第八阶段（测试数据库架构）新增 7 条后为 103 条）。全部文档已统一为上表口径。

## 一、单元测试（Node，CI 可跑 · scripts/unit_test.mjs）

**103/103 通过**，覆盖八个阶段断言：

1. 路线数据（节点数/总里程 27,171km/起终点/字段完整/坐标范围/累计单调）与业务铁律（无假数据/无 1:10/多人默认关/DRAFT/270km 解锁）
2. 后端基础（Fastify/Prisma/JWT/幂等/三态校验/汇总重算）
3. 悦跑圈官方 OAuth 接入（授权 URL/回调/同步/断开/幂等键）
4. 多平台适配器（统一 12 方法契约/not_supported 显式返回/OAuth 1.0a 签名）
5. 接入真实性（八级状态/catalog 12 字段/佳明产品事实/华为资质/微信 Apple 如实标注/令牌 AES-256-GCM 加密/state 一次性防重放）
6. 云端加固（限流/生产 CORS/存储预留/Caddy HTTPS/部署与备份脚本/双清单/悦跑圈 16 步验收表）
7. 仓库接入准备（接入脚本 16 步/保护条款/测过才提交/SHA 一致校验/Backend CI 全要素/lock 无失效镜像/eslint 配置/风险清单/文件清单）
8. 测试数据库架构（TEST_DATABASE_URL/DATABASE_URL 外部库优先/Windows 无 URL 可读失败/无 locale 补丁/不无条件创建 embedded/测试命令三分/CI 用 TEST_DATABASE_URL/force-reset 重复运行确定）

## 二、后端 API 集成测试（backend/tests/api.test.ts）

**31/31 通过**（真实 PG17 + 模拟上游）：认证/班级/活动三态/幂等上传/离线队列/汇总重算 + 三平台 OAuth 全流程（16-22）+ state 重用 400 防重放（23）+ 令牌密文落库（24）+ catalog 五平台状态断言（25）+ webhook×3→501（26）+ 微信小程序未启用 503（27）+ 断开即撤销+凭据清除（28）+ CORS 回显（29）+ 对象存储 not_supported（30）+ 全局限流 429（31）。

## 三、后端静态检查

- `npm run typecheck`（tsc --noEmit）：**零错误**
- `npm run lint`（eslint 9 flat config + typescript-eslint）：**零错误零警告**

## 四、移动端响应式（iPhone 宽度，scripts/mobile_test.cjs）

| 宽度 | 无横向溢出 | 地图渲染 | 双地图 | Kimi预览版标识 | 断言数 |
|---|---|---|---|---|---|
| 375px | ✓ | ✓ | ✓ | ✓ | 24/24 |
| 390px | ✓ | ✓ | ✓ | ✓ | 24/24 |
| 393px | ✓ | ✓ | ✓ | ✓ | 24/24 |
| 430px | ✓ | ✓ | ✓ | ✓ | 24/24 |

含功能流程（测试登录→首页十要素→位置抽屉→手动补录→排行榜→我的）与本地持久化（刷新后数据保留）。

## 五、PWA

- Service Worker 注册成功 ✓（离线基础资源缓存 + 新版本提示）
- manifest standalone ✓ / apple-touch-icon ✓ / viewport-fit=cover ✓ / safe-area CSS ✓

## 六、GitHub Actions YAML（三个工作流）

三个 yml 语法解析通过；仅 kimi/e23-v2-product 触发；无 force / 无生产库 / 无密钥：

- **E23 Android 预览版 APK**（kimi-android-preview.yml）：checkout→Node22→Java21→SDK→ci→tsc→test→build→cap sync→gradle→验APK→apksigner→SHA256→改名→Artifact
- **E23 PWA 部署 GitHub Pages**（kimi-pwa-pages.yml）：VITE_BASE_PATH=/${repo}/ 子路径 + VITE_COMMIT_SHA 注入
- **Kimi Backend CI**（kimi-backend-ci.yml）：postgres:16-alpine service（e23ci 独立测试库，健康检查就绪后执行）→ npm ci → prisma generate → typecheck → lint → migrate deploy → vitest → build → docker build

## 六之二、前端静态检查（本轮补实测）

- `npm run lint`（eslint 9 + typescript-eslint + react-hooks）：**0 错误**（4 条警告，均为 zustand store 依赖的 exhaustive-deps 误报，非阻塞）。修复项：effect 内同步 setState ×4（改微任务/守卫）、未用变量 ^_ 约定、shadcn/ui 模板目录排除。
- `npm run build`（tsc -b 类型检查 + vite）：零错误。

## 七、构建

- 前端：TypeScript 零错误；Vite 产物 JS 437KB（gzip 135KB）+ CSS 91KB
- 后端：`npm run build`（tsc → dist/）零错误；`docker build` 已在 CI 中配置（沙箱无 Docker 守护进程，未本地构建，如实声明）

## 八、Android 工程静态校验

MainActivity（com.e23running.app.kimi.preview）/ AndroidManifest（INTERNET+定位权限）/ Gradle Wrapper（git 100755 可执行位已修正）/ E23 品牌图标+Adaptive Icon / E23 启动屏 / @capacitor/splash-screen 8.0.2 已锁定 / 中文应用名「E23跑起来 Kimi预览版」✓
