# E23跑起来 · 第三阶段验收报告（真实执行，非伪造）

执行时间：2026-07-18 09:43–18:10（UTC+8 沙箱时钟）
验收环境：后端真实 HTTP 服务（tsx src/server.ts，端口 8899）+ 内嵌 PostgreSQL 17.10（端口 55450）+ Prisma 5.22
验收方式：curl 全流程 + vitest 集成测试（app.inject，真实 PG）+ 前端四宽度 Chromium 断言 + 双端构建

## 一、17 项验收结果

| # | 验收项 | 结果 | 实测证据 |
|---|---|---|---|
| 1 | 健康检查 | ✅ | `{"ok":true,"db":"up","serverTime":"2026-07-18T09:43:52.684Z"}` |
| 2 | 管理员登录 | ✅ | 种子账号 13800000001 登录返回 JWT |
| 3 | 邀请码生成 | ✅ | `E23-2FE7D375`（E23-+hex8） |
| 4 | 邀请码注册→pending | ✅ | `{"status":"pending","phone":"139****6666"}`（脱敏正确） |
| 5 | pending 拒贡献 | ✅ | 上传返回 `403 {"error":"NOT_APPROVED"}` |
| 6 | 管理员审批成员 | ✅ | `{"status":"approved"}`，审批后重算班级汇总 |
| 7 | GPS 上传→valid | ✅ | 5km/2000s → `{"status":"valid","pace":400}`，轨迹3点入库 |
| 8 | 幂等重复上传 | ✅ | 同 clientId → `{"duplicated":true}`，进度仍为 5000m 不翻倍 |
| 9 | 异常配速→rejected | ✅ | 10km/1000s → `rejected「配速 100s/km 快于人类极限」`，不计入 |
| 10 | 手动补录→pending | ✅ | `pending「手动补录待人工审核」`，审核前不计入（仍5000m） |
| 11 | 审核通过→计入 | ✅ | approve 后进度 5000→13000m |
| 12 | 删除活动→重算 | ✅ | 删除5km后 13000→8000m |
| 13 | 排行榜仅approved | ✅ | `[{"rank":1,"nickname":"HTTP验收员","totalM":8000}]` |
| 14 | 批量同步≤50（离线回补） | ✅ | 3条（含1重复）→ `{"synced":3,"failed":0,"dups":[false,true,false]}`，进度13000m |
| 15 | 401/403 权限矩阵 | ✅ | 无token 401；无token班级进度 401；成员调admin 403；错密码 401；无效邀请码 `INVITE_INVALID` |
| 16 | 审计日志留痕 | ✅ | 16 条：USER_REGISTERED/USER_APPROVED/INVITE_CREATED/ACTIVITY_VALIDATED×4/ACTIVITY_PENDING/ACTIVITY_REJECTED/ACTIVITY_DELETED/PROGRESS_RECOMPUTED×6 |
| 17 | 双端构建 | ✅ | 后端 `tsc` 0 error + 前端 `vite build` 6.76s 通过 |

## 二、自动化测试汇总（同批执行）

| 套件 | 结果 |
|---|---|
| 后端集成测试（vitest + 真实内嵌 PG17） | 13/13 通过，4.60s |
| 前端单元断言（scripts/unit_test.mjs） | 35/35 通过 |
| 移动端四宽度（375/390/393/430，iPhone UA+触屏） | 各 16/16 通过，加载 <2.1s |

## 三、关键修复记录（本阶段）

1. **embedded-postgres PG18-beta Bus error** → 换装 `embedded-postgres@17.10.0-beta.17`（PG 17.10 二进制），冒烟测试启停正常。
2. **Prisma Client 未生成**：schema 关系字段错误（RouteProgress.userId 为哨兵字符串不能建关系）导致首次 generate 静默失败；移除 User.progresses 反向字段后 generate 成功。
3. **沙箱 /tmp 重启丢失**：后端源码已全部落库 `backend/` 目录纳入 git，不再依赖临时目录。

## 四、如实声明（未完成/受限项）

- 沙箱无 Docker，docker-compose.yml 与 Dockerfile 已做静态校验（YAML 语法/关键指令 grep），未实际构建镜像；已在 GitHub Actions（kimi-backend-ci.yml）中配置真实执行环境。
- 管理端目前仅有 API（curl 可全部操作），管理页面 UI 未做（见《真实数据接入状态表》）。
- 微信授权登录、手表/悦跑圈同步为预留接口，未接入（页面与文档均已明示）。
- 演示模式（sim）记录仅存本机不上传服务端，与 GPS 记录区分。
