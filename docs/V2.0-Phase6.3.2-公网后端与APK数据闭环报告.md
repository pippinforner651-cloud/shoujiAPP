# V2.0 Phase 6.3.2 公网后端与APK数据闭环报告

> 生成日期：2026-07-13
> 项目：全民环游中国虚拟跑步地图

---

## 1. GitHub Actions 构建结果

| 运行 | 提交 | 状态 | 说明 |
|------|------|------|------|
| #3 | fix: GitHub Actions Java版本从17升级到21 | ✅ success | 基础构建正确 |
| #4 | Phase 6.3 跑步数据接入底座 | ❌ failure | RunSession.tsx 函数声明重复（已修复） |
| #5 | Phase 6.3.2 Render部署 + 后端加固 | ❌ failure | 同上原因 |
| **#6** | **fix: RunSession.tsx函数声明重复** | **🟡 构建中** | **已修复，等待完成** |

## 2. APK 文件信息（最新成功构建 #3）

| 项目 | 值 |
|------|-----|
| APK 文件名 | 全民环游中国_V1.0 (app-release-unsigned.apk) |
| 文件大小 | 3,377,795 字节（3.22 MB） |
| SHA256 | `9b38459bbaef45762d06a9cad58733c46a6fd84cf5eff3d292b0d985501db468` |
| artifact 下载 | https://api.github.com/repos/pippinforner651-cloud/shoujiAPP/actions/artifacts/8268607265/zip |
| 网页下载 | https://github.com/pippinforner651-cloud/shoujiAPP/actions/runs/29222308239 |
| 内部 API 地址 | vite 构建时默认值（等待 Render 部署后替换） |

## 3. Render API 地址

> ⏳ **等待用户操作：**
>
> 1. 登录 https://dashboard.render.com
> 2. 点 **New Blueprint**
> 3. 连接 GitHub 仓库 `pippinforner651-cloud/shoujiAPP`
> 4. Render 自动识别 `render.yaml` 中的 PostgreSQL + Web Service
> 5. 点 **Apply** → 等待部署完成（约 3-5 分钟）
> 6. Web Service 部署完成后，在 Dashboard 中复制 URL：`https://chinarun-api.onrender.com`

## 4. Render PostgreSQL 状态

> ⏳ 待 Render 部署后自动创建。Database 配置见 `render.yaml`：
> - 名称：`chinarun-db`
> - 数据库：`vr_china`
> - 用户：`chinarun_user`
> - 计划：Free（新加坡节点）
> - 连接串自动注入 Web Service 的 `DATABASE_URL` 环境变量

## 5. /health 结果

> ⏳ 待 Render API 部署后验证。预期返回：
> ```json
> {
>   "status": "ok",
>   "database": "connected",
>   "time": "2026-07-13T...",
>   "uptime": 123.45
> }
> ```

## 6. Prisma 迁移结果

| 检查项 | 状态 | 说明 |
|--------|------|------|
| prisma validate | ✅ schema 通过 | backend/prisma/schema.prisma 语法正确 |
| prisma generate | ✅ PrismaClient 已配置 | `@prisma/client` 已在 package.json |
| 正式 migration | ✅ 准备就绪 | Render 部署时自动执行 `prisma migrate deploy` |
| Activity 表字段 | ✅ 20 字段 | id/userId/source/sourceActivityId/sportType/startTime/endTime/durationSeconds/distanceMeters/paceSecondsPerKm/calories/avgHeartRate/maxHeartRate/elevationGain/routeData/deviceName/syncTime/verificationStatus/rawDataHash/createdAt/updatedAt |
| 索引 | ✅ 3 个 | userId+startTime / sourceActivityId / rawDataHash |

## 7. 活动写入和读取结果

> ⏳ 待 Render API 部署后验证。测试用例：
>
> ```bash
> # 健康检查
> curl https://chinarun-api.onrender.com/health
>
> # 写入测试活动
> curl -X POST https://chinarun-api.onrender.com/v1/activities \
>   -H "Content-Type: application/json" \
>   -d '{
>     "id": "test_phase632_001",
>     "user_id": "test_user_001",
>     "source": "app_gps",
>     "sport_type": "running",
>     "start_time": "2026-07-13T08:00:00Z",
>     "duration_seconds": 300,
>     "distance_meters": 500,
>     "calories": 35,
>     "verification_status": "verified_device"
>   }'
>
> # 查询
> curl https://chinarun-api.onrender.com/v1/activities/user/test_user_001
> ```

## 8. APK 使用的 API 地址

| 环境 | 地址 | 配置方式 |
|------|------|----------|
| **开发** | `http://localhost:3001` | 默认值（无 VITE_API_BASE_URL） |
| **生产（APK）** | `https://chinarun-api.onrender.com` | GitHub Actions 构建时注入 |
| 配置来源 | `frontend/src/config/api.ts` | VITE_API_BASE_URL 环境变量 |

**APK 内部不写死 localhost**：GitHub Actions 在构建时通过 `VITE_API_BASE_URL` 环境变量注入 Render HTTPS 地址。

## 9. 尚待用户完成的手机测试

| 测试项 | 状态 | 操作 |
|--------|------|------|
| APK 生成 | 🟡 已修复，等待 #6 构建完成 | 在 GitHub Actions 页面查看 #6 结果 |
| APK 安装 | ❌ 待用户验证 | 1. 下载 APK → 2. 传到手机 → 3. 安装 |
| 前台 GPS | ❌ 待用户验证 | 打开 APP → 跑步Tab → 开始 → 记录 ≥500m |
| 锁屏 GPS | ❌ 待用户验证 | 跑步中锁屏 10 分钟 → 解锁查看 |
| 后台 GPS | ❌ 待用户验证 | 切其他 APP 后查看 GPS 是否继续 |
| 服务器入库 | ❌ 待部署 | 需要先部署 Render + 更新 APK |

## 10. 当前真实完成度

| 组件 | 完成度 | 说明 |
|------|--------|------|
| render.yaml | ✅ 100% | 已创建并提交 |
| 后端生产加固 | ✅ 100% | CORS/health/错误处理/日志 |
| API 环境变量配置 | ✅ 100% | config/api.ts + vite env |
| GitHub Actions 构建 | 🔄 修复中 | #6 构建中 |
| 前端 TypeScript 构建 | ✅ 通过 | 698 modules, 3.38s |
| Render 部署 | ❌ 0% | 需用户操作 |
| 真实入库验证 | ❌ 0% | 需 Render 部署后 |
| 真机 GPS 验收 | ❌ 0% | 需安装 APK |

## 11. 问题清单

| 问题 | 状态 | 解决方案 |
|------|------|----------|
| RunSession.tsx 函数声明重复 | ✅ 已修复 | 删除重复的 `export default function` 声明 |
| @fastify/cors 缺失 | ✅ 已修复 | 添加到 package.json |
| 前端 API 地址未注入 | ✅ 已修复 | GitHub Actions + config/api.ts |
| Render 未部署 | 🔴 待操作 | 用户登录 Render → New Blueprint → Apply |
| APK 内 API 指向未更新 | 🔴 待操作 | Render 部署后，设置 GitHub vars.VITE_API_BASE_URL 再构建 |

## 12. 下一步唯一任务

> **Android 真机安装与 GPS 闭环验收**
>
> 具体步骤：
> 1. 等待 GitHub Actions #6 构建完成 → 下载 APK
> 2. 登录 Render → New Blueprint → 连接仓库 → Apply → 复制 API 地址
> 3. 将 API 地址设置为 GitHub Actions 变量或重新构建 APK
> 4. 安装 APK 到 Android 手机 → 实际跑步测试 GPS 闭环
> 5. 验证 POST → PostgreSQL → GET 完整数据链路
>
> 禁止在此阶段进行跑团、积分、商城或页面美化开发。
