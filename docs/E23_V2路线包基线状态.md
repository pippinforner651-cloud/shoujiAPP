# E23 V2路线包基线状态

更新时间：2026-07-15

## 已完成

- V2独立TypeScript类型，不修改V1路线类型和主数据；
- V2总里程下限27,000公里；
- V2固定真实1:1，不引用V1 `SCALE_RATIO`；
- 深圳北大汇丰商学院起终点闭环检查；
- 路段顺序、里程合计和来源引用检查；
- V2 1:1进度、节点边界、完成封顶和超额里程计算；
- 草稿路线不能激活的发布闸门；
- JSON Schema和命令行验证入口；
- `e23-china-loop-v2.draft.json`诚实标记为未完成草稿。

## 保持不变

- `data/route_master_v1.json`；
- V1 21,423公里；
- V1 1:10历史解释；
- 当前正式活动和用户本地历史数据。

## 尚未完成

- 没有生成或宣称已完成真实27,000+公里道路路线；
- 尚未选择道路路由/地图来源；
- 尚未核验学校精确坐标；
- 尚未完成逐段道路、轮渡、里程和来源审计；
- 尚未将V2绑定正式活动。

## 用户确认门

以下事项开始前必须确认：可能付费的路线服务、隐私或位置数据范围、V2路线正式发布、正式活动切换以及不可逆数据迁移。

## 验证命令

```powershell
cd frontend
node --test tests/*.test.ts
npm run build

cd ..
node data\route_packages\validate-route-package.mjs data\route_packages\e23-china-loop-v2.draft.json
```

当前草稿的命令行验证应失败并报告`DISTANCE_SUM_MISMATCH`，这是防止未核验草稿被误用的正确结果。
