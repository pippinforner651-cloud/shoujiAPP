# E23 V2路线包基线状态

更新时间：2026-07-15

## 已完成

- V2独立TypeScript类型，不修改V1路线类型和主数据；
- V2总里程下限27,000公里；
- V2固定真实1:1，不引用V1 `SCALE_RATIO`；
- 深圳北大汇丰商学院名称与集中配置坐标的起终点闭环检查；
- 路线ID、版本、状态、总里程、节点与路段数量基础结构检查；
- 节点顺序、闭环路段指向、累计里程和来源引用检查；
- geometry非空、坐标合法、端点贴合、相邻段连续及几何里程一致性检查；
- 累计与总和误差10米，几何端点250米、相邻断裂500米，几何里程误差上限为1公里或5%（取较大者）；
- V2 1:1进度、节点边界、完成封顶和超额里程计算；
- 未通过全部门禁的草稿不能进入`VERIFIED`、`PUBLISHED`或`ACTIVE`状态；
- JSON Schema和命令行验证入口；
- `e23-china-loop-v2.draft.json`诚实标记为未完成草稿。

## 保持不变

- `data/route_master_v1.json`；
- V1 21,423公里；
- V1 1:10历史解释；
- 当前正式活动和用户本地历史数据。

## 尚未完成

- 没有生成或宣称已完成真实27,000+公里道路路线；
- 尚未选择道路路由/地图来源；
- 当前校园坐标仅作为集中配置的评审基准，正式发布前仍需地图来源复核；
- 尚未完成逐段道路、轮渡、里程和来源审计；
- 尚未将V2绑定正式活动。

## 用户确认门

以下事项开始前必须确认：可能付费的路线服务、隐私或位置数据范围、V2路线正式发布、正式活动切换以及不可逆数据迁移。

## 验证命令

```powershell
cd frontend
node --test tests/*.test.ts
npm run typecheck
npm run build:vite

cd ..
node data\route_packages\validate-route-package.mjs data\route_packages\e23-china-loop-v2.draft.json
```

当前草稿的命令行验证应失败并报告节点、路段或里程门禁问题，这是防止未核验草稿被误用的正确结果。
