# 测试报告（Kimi 独立交付线 · 最终版）

## 环境（如实声明）

- 等效移动端环境：无头 Chromium，iPhone 参数（触屏模式、iOS Safari UA、2/3 倍屏）
- **未在真机 iPhone 上测试；未真实编译 APK；未真实推送 GitHub；Actions 未真实运行**

## 一、单元测试（Node，CI 可跑 · scripts/unit_test.mjs）

24/24 通过：路线数据（节点数/总里程 27,171/起终点/字段完整/坐标范围/累计单调）、业务铁律（无假数据/无 1:10/多人默认关/DRAFT/270km）、登录明示、PWA 文件、Android 包名与权限、无硬编码密钥。

## 二、移动端响应式（iPhone 宽度）

| 宽度 | 无横向溢出 | 地图渲染 | 双地图 | Kimi预览版标识 |
|---|---|---|---|---|
| 375px | ✓ | ✓ | ✓ | ✓ |
| 390px | ✓ | ✓ | ✓ | ✓ |
| 393px | ✓ | ✓ | ✓ | ✓ |
| 430px | ✓ | ✓ | ✓ | ✓ |

## 三、功能流程（390px）

测试登录（13800138000+123456）→ 首页十要素 → 位置抽屉 → 手动补录 5.2km → 排行榜（未启用+本人卡）→ 我的（次数/贡献/DRAFT）全部通过。

## 四、本地数据持久化

注入 3.6km 记录 → 刷新页面 → 数据保留 ✓（localStorage）

## 五、PWA

- Service Worker 注册成功 ✓（离线基础资源缓存 + 新版本提示）
- manifest standalone ✓ / apple-touch-icon ✓ / viewport-fit=cover ✓ / safe-area CSS ✓

## 六、GitHub Actions YAML

两个 yml 语法解析通过；仅 kimi/e23-v2-product 触发；无 force / 无数据库 / 无密钥；步骤齐备（APK 15 步：checkout→Node22→Java21→SDK→ci→tsc→test→build→cap sync→gradle→验APK→apksigner→SHA256→改名→Artifact；Pages 9 步含 VITE_COMMIT_SHA 注入）。

## 七、构建

TypeScript 零错误；Vite 产物 JS 391KB（gzip 121KB）+ CSS 90KB。

## 八、Android 工程静态校验

MainActivity（com.e23running.app.kimi.preview）/ AndroidManifest（INTERNET+定位权限）/ Gradle Wrapper / E23 品牌图标+Adaptive Icon / E23 启动屏 / @capacitor/splash-screen 8.0.2 已锁定 / 中文应用名「E23跑起来 Kimi预览版」✓
