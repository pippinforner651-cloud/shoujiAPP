# 测试报告

## 环境（如实声明）

- 等效移动端环境：无头 Chromium，iPhone 14 参数（视口 390×844、3 倍屏、iOS 17 Safari UA、触屏模式）
- **未在真机 iPhone 上测试**；Safari 特有行为待真机复核
- 被测产物：`npm run build` 输出的 dist/（本地静态服务）

## 自动化测试（Puppeteer 脚本，附仓库 scripts/mobile_test.js）

| # | 项目 | 结果 |
|---|---|---|
| 1 | 页面完整加载耗时 | 通过（≈0.99s） |
| 2 | 测试登录（13800138000 + 验证码 123456） | 通过 |
| 3 | 首页：今日里程 / 今日打卡次数 / 年度目标270km | 通过 |
| 4 | 首页：多人功能尚未上线横幅（无伪造人数/里程） | 通过 |
| 5 | 首页：最近运动动态区域、剩余里程 | 通过 |
| 6 | 位置卡 → 抽屉（所在道路/前往地/距下一城市） | 通过 |
| 7 | 跑步页：手动补录入口、GPS/未实现能力明示 | 通过 |
| 8 | 手动补录 5.2km/30min → 保存 → 总结 | 通过 |
| 9 | 排行榜：未启用提示 + 仅本人真实卡片（5.2km） | 通过 |
| 10 | 我的：运动次数、环线贡献比例、DRAFT 标识 | 通过 |

合计 13 项断言全部通过。

## 构建测试

- `npm run build`：TypeScript 零错误，产物 JS 389KB（gzip 120KB）+ CSS 90KB
- 无运行时外部网络依赖，加载后可离线使用

## 修复的缺陷（本轮）

- 状态订阅返回同一对象引用导致登录后页面不重渲染（store 增加 version 计数修复）
